IMAGE_FILENAME_FORMAT = {
    "front": "{}.png",  # will be formatted with set code if available, otherwise empty string
    "back": "{}.back.png",  # will be formatted with set code if available, otherwise empty string
}
